void main()
{
		int a;
		float b;

		a=0;
		b=0.0;

		printf("Please key in number of [a] in Int-type and [b] in Float-type");
		scanf("%d%f",&a,&b);
		printf("Result    INT: a=%d, b=%d     FLOAT: a=%f, b=%f", a, b, a, b);

		printf("Please key in number of [a] in Float-type and [b] in Int-type");
		scanf("%f%d",&a,&b);
		printf("INT: a=%d, b=%d     FLOAT: a=%f, b=%f", a, b, a, b);

		printf("\nWinnie\nthe\npooh\nis\ncute\n");
}
